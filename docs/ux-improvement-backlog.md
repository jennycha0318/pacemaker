# UI/UX 개선 백로그 (v2)

> 출처: UX ralph loop v2 — 8화면(레이아웃·탭바·로고 / 랜딩 / 인증 / 진단 / 결과 / 히스토리 / 프로필 / 디자인시스템) × 8 UX 차원, 화면별 Explore 에이전트 감사.
> 상태: ✅ 이번 라운드 반영 · ⬜ 미적용(백로그). 민감 도메인(정서 안전) 항목은 ★.

## ✅ 이번 라운드 적용
- ★ **결과 ▲▼ 델타 판정감 완화** — '이렇게 판단했어요' 아래 "점수 계산에 반영된 요인이에요. 당신이 부족하다는 뜻이 아니에요." 캡션 추가.
- **성향·궁합 카드 신뢰도 명확화** — 배지 '참고' → '점수 미반영'.
- **진단 진행바 보정** — `(qIndex/total)` → `((qIndex+1)/total)` (첫 질문부터 채워지고 마지막에 100% 도달).
- **btn-ghost 대비 개선** — 배경 0.45 → 0.68(글래스 위 가독성/WCAG).
- **로고 접근성** — 워드마크와 함께 쓰일 때 로고 `aria-hidden`(스크린리더 'Pacemaker' 중복 읽기 제거).
- **히스토리 상세 맥락** — Report 위에 '단계 · 일시' 메타 추가 + 뒤로가기 버튼화.
- **인증 접근성** — 로그인·회원가입 입력에 `id/htmlFor` 연결 + `autoComplete`(이메일/현재·새 비밀번호/이름) → 비밀번호 매니저·라벨 탭 지원.

## ✅ P1 — 전부 적용 (이번 라운드)
- ★ **결과 섹션 위계/그룹핑** ✅ — 3개 `<section>` 그룹(결과 / 다음 행동 / 근거·참고), 그룹 내 gap 좁게·그룹 간 mt-7, 각 그룹 소제목.
- ★ **위기 상담 카드 가시성** ✅ — needsSupport 시 CrisisResources를 최상단(점수 카드 앞)으로 이동(sticky 미사용, border-2 유지).
- **인증 실시간 검증 + 비밀번호 표시 토글** ✅ — 공용 `PwToggle`(눈 아이콘) + 이메일/비번/일치 onChange 힌트. reset/update도 id·autoComplete 보강.
- **진단 통합 진행 인디케이터** ✅ — `StepIndicator`(내 정보/상황/상대/설문 4단계, 현재 강조·지난 done), 설문 내 질문 진행바 병행.
- **자동진행 오선택 회복** ✅ — 선택 옵션 체크(✓)+'선택됨'+ring, '← 이전' 버튼형 승격으로 직전 수정 용이.
- **히스토리 삭제 기능** ✅ — `DeleteDiagnosisButton`(확인 모달) 목록/상세에 추가(RLS delete 활용).
- **디자인시스템 텍스트 크기 일관화** ✅(부분) — 캡션 11/11.5/12px → **12.5px 통일**(손글씨 가독성·일관성). ⬜ 하드코딩 hex→토큰, 헤더 스케일 체계화, 간격/라운드 정규화는 P2(리팩토링)로 잔류.

## ⬜ P2
- **결과 공유/저장 동선** — 카톡 공유·이미지/PDF·링크. (결과, M) _(로드맵: 결과 공유 이미지)_
- **결과 risks 과다 시 접기** — 낮은 점수일수록 경고 7~8줄 → 아코디언. (결과, M)
- **hold vs msg 시각 구분 강화** — '지금은 기다려요' vs '메시지 예시' 라벨·배경 차별. (결과, S)
- **상대정보 단계 가치 설명** — 입력 시 결과가 뭐가 달라지는지(궁합·소통팁·나이차) 명시. (진단, M)
- **게스트 결과 보존** — localStorage 임시 저장 후 가입 시 복원. (진단, M)
- **히스토리 정렬/필터 + 날짜 그룹핑** — 단계·점수대 필터, 오늘/이번주 그룹. (히스토리, M)
- **프로필 저장 성공 토스트 + 미저장 이탈 경고(beforeunload)**. (프로필, M)
- **인증 reset/update id·autoComplete + 에러 메시지 레이아웃 점프 완화**. (인증, S)
- **하드코딩 hex → 토큰화** — Report/SupportNotices/Google 버튼의 직접 hex를 팔레트 토큰으로. (전역, M)
- **헤더 스케일 체계화** — 화면별 h 크기(34/30/26/25) 산포 → 18px 기준 타이포 스케일. (전역, M)
- **파스텔 위 흰 글씨 대비 검증** — .btn-primary 그라데이션 위 흰 글씨 WCAG 확인. (전역, S)

## ⬜ P3
- **랜딩**: 첫 카드 강조(핵심 가치), 버튼 hover 상태, pt 중복(layout+page) 정리, h1 leading 여유. (S)
- **`<select>` 접근성** — YearSelect/MbtiSelect 네이티브 픽커 한계 → 커스텀/포커스 표기. (인증·프로필, L)
- **프로필 이름 수정불가 안내 + 계정 삭제/탈퇴 동선**(개인정보보호). (프로필, S/L)
- **간격·라운드 정규화** — p-5/gap-3/rounded-[18px]/[14px] 혼재 → 스케일 통일. (전역, S)
- **손글씨 폰트 작은 글씨 가독성** — 11~12px 캡션 Pretendard 폴백/자간. (전역, S)
- **빈 상태 카피 온기** — 히스토리 첫 진단 유도에 격려·안전 메시지. (히스토리, S)
- **TabBar backdrop-blur GPU 부하** — 저사양 대체 옵션. (레이아웃, S)

## 정책·결정 대기 (founder)
- 게스트 저장 유도 카드 배치(Report 위→아래), 법적 고지 일원화 — [pending-decisions](improvement-backlog.md).
- 외도·학대 게이팅, 집착 위험군 쿨다운 등 — [at-risk-protocol](product/at-risk-protocol.md).
